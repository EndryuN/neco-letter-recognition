# NECO INM427 — Letter Recognition, pinned dependencies.
#
# IMPORTANT about PyTorch on HPC:
# The default `pip install torch` wheel below targets CUDA 12.1. If your HPC
# uses a different CUDA version (check with `nvidia-smi` or `module avail cuda`),
# install the matching wheel from https://pytorch.org/get-started/locally/ FIRST,
# for example:
#     pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cu118
# and then install the rest with: pip install -r requirements.txt --no-deps torch
# (Or just let pip resolve: on CPU-only nodes the default wheel works fine.)

torch==2.3.1
torchvision==0.18.1
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
scipy==1.13.1
matplotlib==3.9.1
seaborn==0.13.2
joblib==1.4.2
jupyter==1.0.0
notebook==7.2.1
tqdm==4.66.4
